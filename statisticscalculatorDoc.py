# ========================================================================================== Import Section
# imports the statistics module,for statistical measures like mean, median, mode, and standard deviation

# ============================================================================================ Data Section
# defines a list of numbers called data

# =========================================================================================== Print Section
# statistics.mean(data) calculates the mean (average) of the data list, then printed out along with the label "Mean:"

# statistics.median(data) calculates the median (middle value) of the data list, then printed out along with the label "Median:"

# statistics.mode(data) calculates the mode (most frequently) of the data list, then printed out along with the label "Mode:"

# statistics.stdev(data) calculates the standard deviation (variation or dispersion amount) of the data list, then printed out along with the label "Standard Deviation:"
